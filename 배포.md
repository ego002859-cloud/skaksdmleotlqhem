# MY OS 배포 (3단계, 약 15분)

## 1. Apps Script 교체 (시트 백엔드)
1. https://script.google.com → 기존 MyOS 프로젝트 열기
2. Code.gs 내용 전부 지우고 이 폴더의 `Code.gs` 붙여넣기 → 저장
3. 오른쪽 위 **배포 → 배포 관리 → 연필(편집) → 버전: 새 버전 → 배포**
   - 이렇게 하면 URL이 그대로 유지돼서 단축어·대시보드 설정을 안 바꿔도 됨
   - 새 배포를 만들었다면: 대시보드 설정에 새 URL 붙여넣고, 단축어 URL도 같은 걸로 교체
4. 시트 탭(schedule / todo / log / notes)은 첫 요청 때 자동 생성됨

## 2. GitHub Pages 올리기 (아이폰에서 열기 위해)
1. github.com → New repository → 이름 `myos` → Public → Create
2. "uploading an existing file" 클릭 → 이 폴더의 파일 전부 드래그
   (index.html, manifest.json, sw.js, icon-180.png, icon-192.png, icon-512.png)
   → Commit
3. Settings → Pages → Branch: main / (root) → Save
4. 1~2분 뒤 https://ansghk.github.io/myos/ 로 열림

## 3. 아이폰 홈화면에 추가
1. Safari로 위 주소 열기
2. 공유 버튼 → **홈 화면에 추가** → 추가
3. 홈화면의 MY OS 아이콘으로 열면 주소창 없이 앱처럼 실행됨

## 확인
- 대시보드 왼쪽 아래(모바일은 "현황" 옆)에 **연결됨 · 시간** 이 뜨면 정상
- 단축어 "할일 추가" 실행 → 대시보드 새로고침하면 할일에 나타남
- 비행기 모드로 할일 추가 → "저장 대기 1건" → 비행기 모드 해제 → "연결됨"으로 바뀌면 오프라인 큐 정상
